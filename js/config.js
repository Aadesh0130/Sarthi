/* =========================================================
   Sarthi — frontend configuration.
   Point this at wherever you run the FastAPI backend (see
   /backend). Defaults to a local backend on port 8000.
   ========================================================= */
window.SARTHI_BACKEND_URL = window.SARTHI_BACKEND_URL || "http://localhost:8000";
